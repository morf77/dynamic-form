export const enum FIELDS_TYPE {
  SELECT = 'select',
  TEXT = 'text',
  RADIO = 'radio',
  NUMBER = 'number',
  CHECKBOX = 'checkbox',
  GROUP = 'group',
  DATE = 'date'
}

export enum VISIBILITY_CONDITION {
  EQUALS = 'equals'
}

export enum HTTP_METHOD {
  GET = 'GET',
  POST = 'POST'
}
